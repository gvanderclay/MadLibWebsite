CREATE TABLE MadLibs (
SEQ_ID INT(4) AUTO_INCREMENT PRIMARY KEY, 
TITLE varchar(30) NOT NULL, 
CATEGORY varchar(30) NOT NULL, 
MADLIB_TEXT VARCHAR(5000) NOT NULL, 
RATING INT(1),
REG_DATE TIMESTAMP
);

SELECT SEQ_ID, MADLIB_TEXT, RATING 
FROM MadLibs;

SELECT MADLIB_TEXT, RATING FROM MadLibs 
WHERE SEQ_ID = @ID;

INSERT INTO MadLibs (CATEGORY, MADLIB_TEXT, RATING)
VALUES(0,@Category, @MADLIB_TEXT, @RATING, 0);


